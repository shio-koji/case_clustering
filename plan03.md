# CALL4 ケース分類 — タグ単独トラック（plan03）
# Tag-only Classification Track

> [plan02.md](plan02.md) が「テキスト（概要・本文）を主特徴量」にしたのに対し、
> **plan03 は既存タグ（subject_tags）だけを特徴量**にして、同じケースをいろいろな手法で分類する。
> 目的は「新構造の発見」ではなく、**(1) 誠実なベースライン（タグだけならどこまで分かれるか）、
> (2) タグ体系そのものの批評、(3) タグ由来の混合可視化**。
> 作成: 2026-07-22 ／ 前提: plan01.md（方針）・plan02.md（テキストトラック）

---

## 0. 設計方針（plan02からの継承と差分）

- **特徴量は `tags.npz`（95ケース × 11タグ の二値 multi-hot 行列）のみ**。テキスト・埋め込みは使わない。
- plan02 で A1（bag-of-tags）は「null model」として階層1本に留めた。plan03 はこれを**主役に格上げ**し、
  二値・カテゴリデータに適合する手法を系統的に走らせる。
- **主結果はplan02との比較**：plan03の各手法 vs plan02の勝者（テキスト Leiden K=5 / NMF K=6）を
  ARI/NMI で突き合わせ、「タグだけの構造」と「テキスト由来の構造」のズレを定量化する。
- LLM不使用。命名も plan02 同様「タグ濃縮＋代表ケース」を根拠にし、循環を避ける。
- **過剰主張しない**：下記「情報の天井」を各所に明示する。

### 情報の天井（この分析の限界を最初に固定）

- 特徴は **11タグのみ**。1ケース平均1.6タグ、**ユニークな組合せは37通り**（95件が37点に潰れる）。
  → 距離ベース手法が区別できる点は最大37。新しい微細構造は原理的に出ない。
- **「公正な手続」が43/95件（45%）** に付く**背景タグ**。共不在を無視する Jaccard 系距離を使わないと、
  この支配タグ経由で大半のペアが「似て見える」。
- 孤立タグ：個人情報・プライバシー n=1、沖縄 n=4。単独ではクラスタを作れない。

> バイオ対応：これは発現行列ではなく **11マーカーのフローサイトメトリー／CITE-seqパネル（二値・
> multi-positive）**。「表面マーカーだけで細胞を分ける」問題。連続埋め込み用の手法（k-means/PCA/UMAP）は
> 物差しが合わないので、**二値・カテゴリ専用の手法**を選ぶ。

---

## 1. ディレクトリ構造（plan01/plan02との共存）

共通資産（`cache/`・取得系スクリプト・`plan02/features/tags.npz`）は再利用し、plan03固有物は隔離する。

```text
case_clustering/
├── plan01/ plan02/            # 凍結
├── plan02/features/tags.npz   # ← 共有して読む（95×11タグ行列・case_index）
└── plan03/
    ├── s0_prepare.py          # Stage 0 タグ準備・類似度・分布
    ├── s1_methods.py          # Stage 1 5系統の手法実行
    ├── s2_interpret.py        # Stage 2 タグ濃縮・代表・命名
    ├── s3_evaluate.py         # Stage 3 内的妥当性・手法間ARI・plan02比較・タグ批評
    ├── s4_report.py           # Stage 4 可視化＋HTMLレポート
    ├── features/              # 中間成果物（類似度行列・座標）
    ├── results/               # ラベル・混合比・評価JSON
    └── report/                # HTML・図
```

---

## 2. 手法カタログ（推薦ショートリスト5系統）

二値 multi-label 行列に適合する手法を、目的に効く5本に絞る（全系統の棚卸しは検討メモ参照）。

| # | 系統 | 手法 | 出力 | 実装 |
|---|---|---|---|---|
| 1 | 潜在変数モデル | **LCA / ベルヌーイ混合**（EM・K=2〜8・BIC選択） | ソフト所属＋硬ラベル | 自前EM |
| 2 | 行列分解（混合） | **NMF on タグ**（K=4〜8） | タグ・アーキタイプ混合比 | sklearn |
| 3 | カテゴリ次元圧縮 | **MCA**（複表指標のCA） | ケース＋タグ同一空間座標 | 自前SVD |
| 4 | グラフ | **タグ共起コミュニティ検出**（メタタグ）＋**ケース類似Leiden** | タグの束・ケースのグラフ分割 | igraph+leidenalg |
| 5 | 距離＋共クラスタ | **Jaccard階層**＋**スペクトル共クラスタリング** | 入れ子＋ブロック構造 | scipy+sklearn |

- 参照として **頻出タグ組合せ（frequent itemsets）** も列挙（タグ空間が表現しうる分類の上限）。
- 距離は原則 **Jaccard**（共不在を無視）。理由は各所に明記。
- Kの基準は plan02 に合わせ **5〜6** を軸にスイープ。混合(NMF)の主Kは plan02 NMF と揃えて **K=6** も併算し比較可能に。

---

## 3. ステージ詳細

### Stage 0. タグ準備
- `plan02/features/tags.npz`（行順＝`case_index.json`）を読み、分布・組合せ数・共起行列を出力。
- 二値類似度（Jaccard / Dice / Ochiai）行列と距離行列を作り保存。距離選択の根拠を記録。
- **出力**: `plan03/features/tag_sim.npz`（類似度・距離）、分布サマリ。

### Stage 1. 手法実行（5系統＋参照）
- 上表の5系統を同一タグ行列・シード42で実行。ラベルは plan02 と同じ JSON 形式（`{method, labels, ...}`）で保存。
- **出力**: `plan03/results/labels_*.json`、`membership_nmf_tags.json`・`membership_lca.json`（比率・エントロピー）、
  MCA座標、タグ共起グラフのコミュニティ、frequent itemsets。

### Stage 2. 解釈（タグ濃縮・代表・命名）
- 各クラスタ/アーキタイプについて **タグ濃縮**（クラスタ内タグ率 vs 全体率のリフト）で特徴タグ上位を出す
  （plan02のc-TF-IDFのタグ版）。
- 代表ケース：排他は medoid（Jaccard最小平均）、混合は比率最大の上位3件。
- 命名：濃縮タグ＋代表ケースのみを根拠に短い名前（LLM由来のタグ名を鵜呑みにせず、循環回避の注記）。
- **出力**: `plan03/results/interpretation.json`。

### Stage 3. 評価（3層＋plan02比較）
1. **内的妥当性**: Jaccard距離上のシルエット（タグ空間内でのみ比較。plan02のテキスト空間とは物差しが違う旨明記）。
2. **手法間一致**: plan03内の全手法で ARI/NMI 行列。
3. **plan02との比較（本トラックの主眼）**:
   - plan03各手法 × plan02勝者（`labels_leiden_pca` / NMF優勢トピック）の ARI/NMI。
   - 「タグだけの構造」がテキスト由来6軸をどれだけ再現するか＝タグの説明力の定量化。
4. **タグ批評**: 支配タグ（公正な手続の"その他"化）、冗長タグ（共起コミュニティで束になる組）、
   孤立タグ、統合・分裂の具体列挙。
- **出力**: `plan03/results/evaluation.json`。

### Stage 4. 可視化・レポート
| 図 | 役割 |
|---|---|
| タグ分布バー＋共起ヒートマップ | データの素性 |
| **MCAバイプロット**（ケース＋タグ） | タグ空間の地図（UMAPのタグ版） |
| **タグ共起ネットワーク**（メタタグ色分け） | タグ体系批評の中心図 |
| NMF/LCA 混合比スタックバー | タグ由来の混合可視化 |
| スペクトル共クラスタ ヒートマップ | ケース群×タグ群のブロック構造 |
| **plan03↔plan02 ARIヒートマップ** | 「タグ vs テキスト」の一致要約（主結果） |
- **単一自己完結HTML・日英併記**（plan01/plan02の成果物要件を踏襲）。plan02とは別Artifact。
- **出力**: `plan03/report/call4_plan03_report.html`。

---

## 4. ロードマップ

| # | ステージ | 主な成果物 |
|---|---|---|
| 1 | Stage 0 準備 | 類似度・距離行列、分布 |
| 2 | Stage 1 手法 | 全ラベル・混合比・MCA座標・共起コミュニティ |
| 3 | Stage 2 解釈 | タグ濃縮・代表・命名 |
| 4 | Stage 3 評価 | 内的・手法間・**plan02比較**・タグ批評 |
| 5 | Stage 4 報告 | 図一式＋HTMLレポート |

依頼者の指示により **plan03 は一気通貫で実行**し、完了後に所見をまとめて提示する
（plan02のようなステージ毎レビューは挟まない。要修正点は事後に反映）。

## 5. 対象外
- テキスト・埋め込みの再利用（それは plan02 の領分）
- 新しいタグの自動生成・タグ推薦モデル（次期計画）

---

## 付録：実施結果（2026-07-22 完了・一気通貫）

### 実行したもの
- `s0_prepare.py` → `s1_methods.py` → `s2_interpret.py` → `s3_evaluate.py` → `s4_report.py`（seed=42固定）。
- 特徴＝`plan02/features/tags.npz`（95×11二値）。距離＝Jaccard主。依存追加なし
  （MCA・ベルヌーイ混合は自前実装、既存のsklearn/scipy/igraph/leidenalgのみ）。
- 出力: `plan03/features/{tag_sim,mca}.npz`・`plan03/results/*.json`・
  `plan03/report/call4_plan03_report.html`（255KB・日英併記・自己完結）。

### 主な発見
1. **情報の天井が実証された**: 95件→**ユニークなタグ組合せ37通り**、**68%のケースペアがタグを1つも共有しない**。
   タグ単独では微細構造は原理的に出ない＝ベースライン／批評ツールとしての位置づけが正しい。
2. **plan02（テキスト）との比較が主結果**: タグNMF ↔ テキストNMF が **ARI 0.40** で最大一致、
   テキストLeiden(K=5)には最良でも **ARI 0.32**。→ タグはテキスト構造の**約1/3を説明する「参照的一致」**。
   plan02の「テキストNMFがタグと最整合(ARI 0.335)」と表裏一体で、**タグとテキストは部分的に重なるが
   互いを再現しない別々の情報源**という結論を両面から確認。
3. **タグ体系批評（定量）**:
   - **背景タグ**: 「公正な手続」(43件)は**テキスト6トピック全てに出現**＝実質「その他」。
   - **分裂タグ**: 働き方・政治参加・表現の自由・外国ルーツ・ジェンダー・医療福祉が4〜5トピックに分散
     ＝単独では分類軸として弱い。
   - **冗長タグ（メタタグ）**: 共起コミュニティが束ねた組
     ＝｛公正な手続＋環境・災害｝｛刑事司法＋医療・福祉・障がい｝｛個人情報＋政治参加｝
     ｛ジェンダー＋働き方＋外国ルーツ｝。統合の候補。
   - **孤立タグ**: 個人情報・プライバシー(n=1)・沖縄(n=4)は単独でクラスタを作れない。
4. **手法の当落**: 距離ベースの**Jaccard階層は「公正な手続」の巨大塊(約6割)に退化**（plan02 null modelと同じ病理）。
   混合(NMF)・グラフ(Leiden)・共クラスタはよりバランス良。**LCAのBIC最適はK=2**
   （タグ情報はそもそも多クラスを強く支持しない）。

### 注意・限界（レポートにも明記）
- タグ空間のシルエット(0.3〜0.4)は離散・重複点ゆえ高く出る。**plan02の埋め込み空間0.12前後とは比較不可**。
- 命名に使うタグ濃縮(lift)は希少タグを過大評価しやすい（名前は目安）。
- K=6は plan02 と比較するための便宜的固定値（BIC最適ではない）。

### 未実施・依頼者判断待ち
- **Artifact公開は保留**（plan02同様、別Artifactとする方針は維持）。
- frequent itemsets / FCA（束論）は設計に残置したが今回は未実装（必要なら追加）。
