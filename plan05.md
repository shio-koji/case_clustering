# CALL4 ケース分類 — ソフトタグ・トラック（plan05）
# Soft-tagging track: per-case tag weights & missing-tag detection

> 系統B（テキスト埋め込みによるソフトタグ採点）。plan02 の bge-m3 埋め込みを使い、
> **「1ケースの中で各タグがどれだけ中心的か（割合）」**と**「貼り忘れ／過剰タグの候補」**を出す。
> 前提: plan01/02/03。作成: 2026-07-24。LLM不使用。

---

## 0. 動機と問い

生の二値タグ（0/1）には「重み」の情報が無い。ケースが複数タグを持つとき、
どのタグがそのケースの“主題”でどれが“傍論”かを、**テキストの中身から**推定したい。
- **問1（割合）**: ケース i が持つタグ集合の中で、各タグの相対的な重み（合計1）はいくつか？
- **問2（貼り忘れ）**: ケース i が**持っていない**タグのうち、テキスト上は強く該当する（貼るべき）ものは？
- （副産物）**過剰タグ**: 付いてはいるがテキスト上の該当が弱いタグ。

---

## 1. 方法（系統B-2：判別的プロトタイプ採点）

### 1.1 なぜ素のコサインではダメか
bge-m3 埋め込みはコサイン下限が高い（ペア中央値0.57）。「全ケースが法律文書として似ている」
共通成分が乗るため、`cos(ケース, タグ重心)` はどのタグでも0.5〜0.7に張り付き差が出ない。

### 1.2 判別的スコア（common-mode 除去）
各タグ t について、**持つ群の重心 pos と 持たない群の重心 neg** を作り、その差で採点：

```
disc(i, t) = cos(emb_i, pos_protoₜ) − cos(emb_i, neg_protoₜ)
```

- `pos_protoₜ` = タグtを持つケースの埋め込み平均（L2再正規化）
- `neg_protoₜ` = タグtを持たないケースの埋め込み平均（L2再正規化）
- 共通成分が pos/neg 両方に等しく乗るので差し引きで消え、**タグt固有の向き**が残る。
  最近傍重心分類（nearest-centroid）／簡易LDAの決定値に相当。`disc>0`＝「tらしい」。

### 1.3 自己参照の除去（Leave-One-Out）
ケース i が自分でタグtの重心を押し上げる水増しを避けるため、
**i がタグtを持つ場合は pos_protoₜ を i を除いて計算**（LOO）。持たない場合は全体重心でよい。

### 1.4 割合（問1）
ケース i の**付与済みタグ**についてのみ、判別スコアを softmax で正規化：

```
ratio(i, t) = softmax_{t∈Tᵢ}( disc(i,t) / τ )      （Σ=1）
```

- τ（温度）はデータの disc 分布から自動設定（付与タグ disc の標準偏差）。恣意性を避けるため値を報告。
- 直感：そのケースが「どのタグの向きに一番寄っているか」の相対配分。

### 1.5 貼り忘れ・過剰タグ（問2）
- **貼り忘れ候補**: `i がタグtを持たない` かつ `disc(i,t)` が高い。
  タグtの陽性ケースの disc 分布（LOO）に対する**パーセンタイル**で基準化し、
  「実際にtが付いているケースの下位25%より上」＝候補として順位化。
- **過剰タグ候補**: `i がタグtを持つ` のに `disc(i,t)` が陰性群並みに低い。
- **信頼度**: 陽性が2件未満のタグ（個人情報 n=1）はプロトタイプが作れずLOO不能 → **低信頼で除外／注記**。
  n<3 のタグ（沖縄 n=4 等）は候補提示するが「低信頼」ラベルを付す。

---

## 2. 検証（この採点を信じてよいか）

埋め込みが「タグを言い当てられる」ことを先に示さないと、貼り忘れ提案は絵に描いた餅。
- **タグ別 ROC-AUC**: LOO disc が陽性(t保有)と陰性(t非保有)をどれだけ分離できるか。
  AUC 高いタグ＝そのタグは埋め込みでよく捉えられており、貼り忘れ提案が信頼できる。
- **自己再現（top-k recovery）**: 各ケースについて disc 上位k個に、実際の付与タグが入っているか。
- これらが弱いタグ（例：横断的な「公正な手続」）は「テキストから復元しにくい＝提案も控えめに」。

---

## 3. 出力とレポート

- `plan05/results/soft_tags.json`: disc 行列・ratio・貼り忘れ／過剰候補・検証（AUC・recovery）。
- `plan05/report/call4_plan05_report.html`（単一自己完結・日英併記）:
  - 手法解説（なぜ判別スコア＋LOOか）
  - **ケース内タグ割合**（代表ケースのスタックバー／全ケール表・CALL4リンク）
  - **貼り忘れ候補**ランキング（ケース→提案タグ＋根拠：近い陽性ケース）
  - **過剰タグ候補**
  - 検証（タグ別AUCバー・recovery）
  - 限界（bge-m3依存・N小・横断タグは復元困難・因果でない）

- スクリプト: `plan05/s0_softtag.py`（計算）・`plan05/s1_report.py`（HTML）。seed=42。
  埋め込みは `plan02/features/emb.npz` を再利用（再計算しない）。

## 4. 限界（先に明示）
- 重みの根拠は**テキストと埋め込みモデル**であり、法的な重要度そのものではない。
- 貼り忘れ提案は**仮説**（人手レビュー前提）。特に横断タグ・希少タグは低信頼。
- N=95・タグ11の小規模。過剰主張しない。
