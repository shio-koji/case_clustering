# CALL4 ケース分類 — 段階実行計画（plan02）

> [methodology_review.md](methodology_review.md) の軸A/B/C（刈り込み済み）を土台に、
> **「特徴表現化 → 次元圧縮 → 構造発見 → 解釈 → 評価 → 可視化」を段階的に実行する**計画。
> 各ステージは独立したスクリプト＋中間成果物を持ち、**ステージごとに依頼者レビューを挟んで**次へ進む。
> 作成: 2026-07-21 ／ 前提: [plan01.md](plan01.md)（プロジェクト方針）、methodology_review.md（手法棚卸し）

---

## 0. 設計方針（確認済みの「芯」）

- **混合メンバーシップ（基底ベクトル）は重要な成果候補**だが、**特定の表現×手法の組み合わせを事前に主役とは決めない**。排他・混合の全組み合わせを対等に比較し、結果を見て採用を判断する。
- **力点はLLM不使用のデータ駆動**。LLM構造抽出（軸A）は最終ステージの比較・補完として実施。
- **既存タグは2通り**：de novo（使わない）で構造を立ち上げ、参照（使う）で答え合わせ・タグ批評。
- クラスタリングは**共通の縮約空間**で行う（高次元のまま回さない）。
- クラスタ命名は**c-TF-IDF説明語＋medoid**を根拠にし、既存タグの多数決は使わない（循環回避）。

---

## 1. 全体アーキテクチャ

```text
Stage 0        Stage 1           Stage 2         Stage 3            Stage 4         Stage 5
データ準備  →  特徴表現化     →  次元圧縮     →  構造発見        →  解釈・評価   →  可視化・報告
(形態素含む)   A1 タグ(参照)     SVD (TF-IDF)    B-hard: k-means     説明語・命名     共通UMAP
               A2 TF-IDF         PCA (埋め込み)  B-hard: 階層        medoid           スタックバー
               A3 bge-m3埋込     (LDA/NMF比率    B-hard: Leiden      ARI/NMI行列      Sankey
               A4 LLM構造抽出     は縮約不要)    B-soft: NMF/LDA     タグ対応         デンドログラム
               (最後に実施)                      B-複合: BERTopic    安定性           HTMLレポート
```

- 各ステージの出力は**共通フォーマットで保存**し、後段はそれだけを入力にする（前段の再実行不要）。
- スクリプトは `plan02/` 配下にステージ番号で置く（§1b参照）。

## 1b. ディレクトリ構造（plan01との共存）

**共通資産はルート、plan固有物は各ディレクトリに隔離**する。plan01の成果物は `plan01/` に避難済み（凍結）。

```text
case_clustering/
├── README.md / plan01.md / plan02.md / methodology_review.md
├── mcp-bridge/
│
├── 01_fetch_data.py          # 共通: データ取得（両planの入口）
├── 02_build_corpus.py        # 共通: コーパス構築 → cache/corpus_clean.json
├── cache/                    # 共通: 生データJSON＋corpus（git追跡）
│   └── *.pkl                 #   派生バイナリ（gitignore）。※plan01の埋め込みキャッシュはアップデート込みテキスト由来のため plan02 では再利用しない
│
├── plan01/                   # plan01専用（凍結。再実行する場合はリポジトリルートから）
│   ├── 03_analyze.py
│   ├── 04_generate_report.py
│   └── results/              #   旧レポート・解析結果
│
└── plan02/                   # 本計画
    ├── s0_tokenize.py        # Stage 0
    ├── s1_features.py        # Stage 1
    ├── s2_reduce.py          # Stage 2
    ├── s3_structures.py      # Stage 3
    ├── s4_interpret.py       # Stage 4
    ├── s5_llm_facets.py      # Stage 4.5
    ├── s6_evaluate.py        # Stage 5
    ├── s7_report.py          # Stage 6
    ├── features/             # Stage 0–2 中間成果物（バイナリはgitignore、JSONは追跡）
    ├── results/              # Stage 3–5 出力（JSON・git追跡）
    └── report/               # Stage 6 出力（HTML・図）
```

- **共有して使うもの**: `cache/` の生データJSON・`corpus_clean.json`・bge-m3埋め込みキャッシュ、
  取得系スクリプト（01・02）。これらは動かさない。
- **plan01専用として避難したもの**: 解析・レポートスクリプト（03・04）と `plan01/results/`
  （旧HTMLレポート・analysis_results.json）。パス定数は移動先に合わせて修正済み。
- plan02 の出力はすべて `plan02/` 配下に閉じるため、両planの成果物が衝突しない。

---

## 2. 軸の組み合わせ表（何と何を掛けるか）

軸A（表現）×軸B（構造発見）は総当たりせず、**意味のある組み合わせに限定**する。

| 表現 ＼ 構造 | k-means | 階層+デンドログラム | KNN+Leiden | NMF/LDA（混合） | BERTopic |
|---|:-:|:-:|:-:|:-:|:-:|
| A1 既存タグ (bag-of-tags) | — | ○(参考) | — | — | — |
| A2 TF-IDF（形態素）→SVD | ○ | ○ | ○ | ○（生TF-IDF/Countに適用） | — |
| A3 bge-m3埋め込み→PCA | ○ | ○ | ○ | —（非負でないため不可） | ○ |
| A4 LLM構造抽出（facet） | — | ○(参考) | — | ○（facet自体が混合基底） | — |

- **○ = 実施する組み合わせ（全て対等な比較対象）**。— = 実施しない。どれを採用するかは結果を見て判断する。
- A1は「人手タグだけでどこまで構造が出るか」の**null model**として階層のみ（タグJaccard距離）。
- NMFは埋め込みに適用できない（負値）ため、混合表現の材料は**語彙系（A2）が担う**。
  意味系（A3）はBERTopic・排他クラスタ・可視化座標（UMAP）が担う。これは優劣ではなく**技術的な役割分担**。
- A4（LLM構造抽出）は Stage 4.5 として最後に実施し、「LLMなしで出た構造」と突き合わせる。

---

## 3. ステージ詳細

### Stage 0. データ準備（既存資産の確認＋形態素）

**入力**: `cache/corpus_clean.json`（95件、取得済み）
**やること**:
- テキスト構成の確定：**ケース名＋概要＋本文（contents）。アップデートは含めない**（確定済み）。
- **日本語形態素解析の導入**（SudachiPy or fugashi/MeCab）。名詞・動詞・形容詞を基本形で抽出、
  ストップワード（訴訟一般語：「裁判」「原告」「被告」等の頻出定型語）はここで候補リスト化。
- 欠損・長さ分布の確認表を出力。

**出力**: `plan02/features/tokens.json`（ケースごとのトークン列）、ストップワード案
**チェックポイント**: トークン化の品質を数件目視（複合語の切れ方、固有名詞）。ストップワード案を依頼者確認。

---

### Stage 1. 特徴表現化（複数手法を並列に作る）

**入力**: Stage 0 の出力
**やること**: 4つの表現を**同一ケース順の行列**として生成・保存する。

| ID | 表現 | 生成方法 | 形状(目安) |
|---|---|---|---|
| A1 | bag-of-tags | 既存タグのone-hot（参照・null model用） | 95 × ~11 |
| A2 | TF-IDF | 形態素1-gram＋2-gram、min_df=2、max_df=0.90、sublinear_tf、L2正規化。**Count行列も別途保存**（LDA用） | 95 × 数千 |
| A3 | 埋め込み | bge-m3で**Stage 0確定テキストを再エンコード**（旧キャッシュはアップデート込みのため不使用）、L2正規化 | 95 × 1024 |
| A4 | LLM構造抽出 | ※Stage 4.5 に後置（下記） | 95 × facet数 |

**出力**: `plan02/features/{tags,tfidf,count,emb}.npz`＋語彙リスト
**チェックポイント**: TF-IDF上位語を数クラスタ分眺めて「法律名・制度名が拾えているか」を確認。

---

### Stage 2. 次元圧縮（共通縮約空間を作る）

**入力**: Stage 1 の行列
**やること**:
- A2: TruncatedSVD。**累積寄与率80–90%を目安に次元決定**（~50–100）。再L2正規化。
- A3: PCA。同じく寄与率基準（~30–50）。再L2正規化。
- **実証ミニ実験**: A3について「1024次元のまま vs PCA後」でk-meansのシルエット・タグARIを比較し、
  圧縮の効果を1枚の図にする（論点1のエビデンス）。
- スクリープロット（寄与率の減衰）も保存。

**出力**: `plan02/features/{tfidf_svd,emb_pca}.npz`、寄与率・比較図
**チェックポイント**: 採用次元数と寄与率、実証実験の結果を依頼者に提示 → 以後この空間で固定。

---

### Stage 3. 構造発見（縮約空間の上で、排他と混合を並走）

**入力**: Stage 2 の縮約行列（＋NMF/LDAのみ生TF-IDF/Count）

**3a. 排他クラスタリング（比較用グリッド）** — 全て同一縮約空間・シード固定:
| 手法 | 設定 |
|---|---|
| k-means | k=5基準（4–8スイープ）、n_init=50 |
| 階層 | コサイン距離＋average linkage、樹形図保存、k=5で切断 |
| KNN+Leiden | 近傍10、重み=コサイン類似度、resolution {0.25, 0.5, 0.75, 1.0, 1.5} → クラスタ数5に最も近い設定を採用 |
| BERTopic（A3のみ） | 埋め込み→UMAP(5次元)→HDBSCAN→c-TF-IDF。外れ値(-1)は削除せず報告 |
| （null model）A1階層 | タグJaccard距離＋average。「タグだけで見た構造」の参考 |

**3b. 混合メンバーシップ**:
| 手法 | 入力 | 設定 |
|---|---|---|
| **NMF** | TF-IDF（生・非負） | K=4〜10スイープ。再構成誤差の肘＋トピック解釈性で選択。比率はL1正規化 |
| LDA | Count行列 | K=5基準（NMFの採用Kに揃えて比較）。反復20+ |

保存物：ケース×トピック比率行列、各トピック高荷重語（上位10）、優勢トピック、
比率エントロピー（多ラベル度の指標：高い＝複数論点を横断する境界ケース候補）。

**出力**: `plan02/results/labels_*.json`（全手法のラベル）、`plan02/results/membership_{nmf,lda}.json`
**チェックポイント**: NMFのK選択（誤差カーブ＋各Kのトピック語一覧）を依頼者と確認して採用Kを決定。

---

### Stage 4. 解釈（説明語・代表・命名）

**入力**: Stage 3 の全ラベル・比率
**やること**（全手法に共通の手続きで）:
- **説明語**: クラスタ/トピックごとに形態素ベースc-TF-IDFで上位5–10語。
- **代表ケース**: 排他クラスタは**medoid**（クラスタ内平均距離最小の実在ケース）。
  混合トピックは**比率最大のケース**上位3件。
- **命名**: 説明語＋代表ケースのみを根拠に短い名前を付ける（既存タグの多数決は使わない）。
  LLMで命名する場合は「どの語・どのケースを根拠にしたか」を必ず併記。

**出力**: `plan02/results/interpretation.json`（説明語・代表・命名・根拠）
**チェックポイント**: 各クラスタ/トピックを「一文で説明できるか」を依頼者がレビュー。
説明不能なものは Stage 3 のパラメータ再考のシグナル。

---

### Stage 4.5. LLM構造抽出（A4・比較トラック）

**入力**: 生テキスト（corpus_clean）
**やること**: 各ケースからLLMで多facetを構造抽出：
`法分野 / 原告類型 / 被告（行政・国・私人）/ 争点となる権利・条文 / 求める救済 / 審級`。
- facetベクトル化（multi-hot）→ それ自体が**解釈可能な混合基底**。
- 抽出の検証：無作為10件を人間（依頼者）が突き合わせ、抽出精度を見積もる。

**出力**: `plan02/features/llm_facets.json`＋facet行列
**チェックポイント**: 抽出サンプルの正確性レビュー → 問題なければ Stage 5 の比較に投入。

---

### Stage 5. 評価（3層で）

**入力**: 全ラベル・比率・facet
1. **内的妥当性**: シルエット等は**同じ表現空間内でのみ**比較（空間をまたぐ横並び禁止）。
2. **手法間一致**: 全手法（排他＋混合のargmax＋BERTopic＋A4 facetの主要ラベル）で
   **ARI/NMI一致行列**を作成。複数手法で再現される群＝頑健な構造の候補。
   ブートストラップ安定性（サブサンプル→ARI分布）を主要手法に実施。
3. **既存タグとの対応（使う版）**:
   - クラスタ/トピック × タグの**クロス集計・混同分析**
   - **タグをまたぐ統合**（複数タグが1構造に融合＝タグが細かすぎる候補）と
     **1タグ内の分裂**（1タグが複数構造に割れる＝粗すぎる/別軸が潜む候補）を定量指摘
   - タグJaccard類似度（同一クラスタ内ペア）

**出力**: `plan02/results/evaluation.json`、ARIヒートマップ用データ
**チェックポイント**: 「どの構造が頑健か」「タグ体系のどこに軋みがあるか」の中間所見を文書化し依頼者確認。

---

### Stage 6. 可視化・レポート（軸Cを成果に落とす）

**入力**: これまでの全成果物
**作る図**（methodology_review.md 軸Cから採用）:
| 図 | 役割 |
|---|---|
| **混合比スタックバー（積み上げ棒）** | 全95件×K色でNMF混合比を個票表示。優勢トピック順に整列 |
| トピック×ケース ヒートマップ | 混合構造の全体パターン |
| **タグ↔クラスタ Sankey** | タグ批評の中心図（統合・分裂が一目） |
| 共通UMAP（A3座標に全手法の色を切替） | 手法比較の地図。※「座標は埋め込み由来」と注記 |
| デンドログラム（A2階層・A3階層・A1タグ階層） | 入れ子構造とnull modelの対比 |
| ARIヒートマップ | 手法間一致の要約 |
| ネットワーク図（KNN、混合比で色ブレンド） | 対外説明用（余力があれば） |

**レポート**（単一HTML・日英併記・自己完結、plan01.mdの成果物要件を踏襲）:
データ概要 / 手法解説（前提・限界つき）/ 各ステージの結果 / 評価3層 / タグ体系への示唆 /
限界と次の一手 / 再現情報（モデル・パラメータ・シード）。

---

## 4. ロードマップ（実行順とレビューポイント）

| # | ステージ | 主な成果物 | レビュー（依頼者判断が要る点） |
|---|---|---|---|
| 1 | Stage 0 データ準備 | トークン・ストップワード案 | ストップワード承認、テキスト構成の固定 |
| 2 | Stage 1 特徴表現化 | 表現行列4種（A4除く） | TF-IDF上位語の品質 |
| 3 | Stage 2 次元圧縮 | 縮約行列＋圧縮効果の実証図 | 採用次元の確定 |
| 4 | Stage 3 構造発見 | 全ラベル＋NMF/LDA混合比 | **NMFのK選択**（最重要の判断点） |
| 5 | Stage 4 解釈 | 説明語・medoid・命名 | 「一文で説明できるか」レビュー |
| 6 | Stage 4.5 LLM抽出 | facet行列＋精度見積もり | 抽出サンプルの妥当性 |
| 7 | Stage 5 評価 | ARI行列・タグ混同・安定性 | 中間所見の確認 |
| 8 | Stage 6 可視化・報告 | 図一式＋HTMLレポート | 最終レビュー |

- **1回のセッションで1〜2ステージ**を目安に進める（各ステージが中間成果物で完結するため中断・再開が容易）。
- 手戻りが起きやすいのは「Stage 3のK選択」と「Stage 4の解釈不能クラスタ」。どちらもチェックポイントで
  吸収する設計。
- 旧パイプラインは `plan01/` に凍結済み（03・04と旧results。§1b参照）。plan02のレポートは
  **plan01とは別の新規Artifactとして公開**する（plan01のArtifact/URLはそのまま残す）。

---

## 5. 対象外（今回やらないこと）

- 複数埋め込みモデルの比較（bge-m3 1本に固定）
- 訴状・判決文など添付文書全文への拡張（`list_documents`/`fetch_document_text`。次期計画で）
- HDBSCAN単体・教師あり混同分析・LLMタクソノミー誘導（軸の刈り込みで除外）
- 類似ケース検索UI（主目的でないため、レポート内の数例掲載に留める）

---

## 6. リスクと対処

| リスク | 対処 |
|---|---|
| N=95でトピックが不安定 | Kを小さめから、ブートストラップ安定性で確認、過剰主張しない但し書き |
| 形態素でも訴訟定型語が支配 | ストップワード反復調整（Stage 0のチェックポイントで吸収） |
| NMFトピックが解釈不能 | K再選択 → それでも駄目ならLDA・A4 facetに比重を移す（フォールバック明記） |
| BERTopicが大半を外れ値化 | min_cluster_sizeを一度だけ調整、結果は「外れ値の多さ」自体を知見として報告 |

---

## 付録：Stage 0 実施メモ（次の着手タスク）

Stage 0 は「**以降の全ステージが読む入力を1回で確定させる**」段。ここの品質が語彙系表現
（TF-IDF / NMF / LDA）の質をそのまま決める。スクリプトは `plan02/s0_tokenize.py`。

### タスク一覧（実施順）

| # | タスク | 内容 | なぜ必要か |
|---|---|---|---|
| 0-1 | テキスト構成の固定 | ケース名＋概要＋本文（contents）を連結。**アップデートは含めない**。連結形式（見出し付き）もここで固定 | 以後の全表現が同じ入力を共有する前提を作る。途中で変えると全段やり直し |
| 0-2 | テキスト正規化 | 全角/半角・空白・記号の統一（NFKC）。HTML除去は corpus_clean 構築時に済んでいるため確認のみ | 表記ゆれは形態素解析の誤りと語彙の分裂（同じ語が別トークン化）を生む |
| 0-3 | 形態素解析の導入 | **SudachiPy（モードC=長単位）を第一候補**に導入し、全95件をトークン化 | 文字n-gramのノイズ語（`//` 等）を脱却し「語」で解釈できるようにする。長単位なら「情報公開請求」等の複合語が1語で残りやすい |
| 0-4 | 品詞フィルタ＋基本形化 | 名詞・動詞・形容詞のみ残し、活用形は基本形（辞書形）に統一。1文字トークン・数値は除外 | 助詞・助動詞は分類に寄与せず次元だけ増やす。基本形化で「訴え/訴える」等の分裂を防ぐ |
| 0-5 | ストップワード候補の作成 | 文書頻度（DF）が高い語を機械的にリストアップ（例: DF>80%）し、「訴訟一般語」（裁判・原告・被告・訴訟・裁判所 等）の候補として提示 | 全ケースに出る語はクラスタを分ける力がないのに重みを食う。**ただし削除の最終判断は依頼者**（例:「憲法」は定型語か争点語か、ドメイン判断が要る） |
| 0-6 | 品質確認表の出力 | ①トークン化サンプル数件（複合語・固有名詞の切れ方）②ケースごとのトークン数分布 ③DF上位語リスト | チェックポイント（下記）の材料 |

### 出力

- `plan02/features/tokens.json` — ケースごとのトークン列（以後の語彙系表現はこれだけを読む）
- `plan02/features/stopwords_proposal.json` — ストップワード候補（DF付き）
- 品質確認表（トークン化サンプル・分布・DF上位語）

### チェックポイント（依頼者に確認すること）

1. **トークン化の品質**: 法律名・制度名・固有名詞が意味の通る単位で切れているか（数件の目視サンプルを提示）
2. **ストップワード案の承認**: 機械抽出した候補リストから、削る語・残す語を判断してもらう
3. ここで承認された tokens + stopwords を**凍結**し、Stage 1 へ進む

### 補足（技術選定の理由）

- **SudachiPy** を第一候補にするのは、pip だけで入り辞書同梱（MeCabのような外部辞書設定が不要）、
  かつ分割単位を A/B/C から選べるため。複合語を保ちたい本件はモードC が合う。
  導入に問題があれば fugashi（MeCab系）にフォールバック。
- 表記ゆれ正規化（NFKC）を形態素解析の**前**に行うのは、解析器の辞書引きが表記に敏感なため。

### 実施結果（2026-07-21 完了・承認済み）

- SudachiPy モードC で95件をトークン化（中央値857語/ケース）。複合語（高等裁判所・同性婚・入国管理 等）の
  切れ方を目視確認し品質良好。
- **凍結ストップワード（20語）**: DF≥80%の18語（こと・訴訟・ため・もの・よる・いう・裁判・対する・
  求める・つく・行う・弁護士・原告・支援・費用・考える・思う・問題）＋寄付定型文由来の2語（使途・弁護団）。
  「憲法」等の境界語はIDF減衰に任せる。
- 出力: `plan02/features/tokens.json`・`stopwords_proposal.json`（コミット済み）。

---

## 付録：Stage 1 実施メモ（次の着手タスク）

Stage 1 は「**同一ケース順（ID昇順）の特徴行列を、比較する表現の数だけ並列に作る**」段。
以後の Stage 2〜3 はこの行列群だけを読む。スクリプトは `plan02/s1_features.py`。

### タスク一覧（実施順）

| # | タスク | 内容 | なぜ必要か |
|---|---|---|---|
| 1-0 | 凍結資産の読み込み | `tokens.json`＋承認済みストップワード20語を適用。ケース順（ID昇順）を全行列で共有 | 表現間の行番号ずれは以後の全比較（ARI等）を壊すため、ここで一元管理 |
| 1-1 | A1: bag-of-tags | 既存タグ（subject_tags）のone-hot行列（95×~11） | null model。「人手タグだけの構造」の基準線 |
| 1-2 | A2: 語彙構築 | トークン列から1-gram＋隣接2-gramを生成し、min_df=2・max_df=0.90で語彙確定 | 2-gramで「情報+公開」等の共起概念を拾う。min_dfで1件だけの固有語を、max_dfで残存定型語を落とす |
| 1-3 | A2: TF-IDF＋Count | 同一語彙で **TF-IDF行列**（sublinear_tf、L2正規化）と**Count行列**（LDA用・正規化なし）の2本を保存 | TF-IDFはSVD/NMF/クラスタリング用、生CountはLDAの前提（LDAは頻度の生成モデルのため） |
| 1-4 | A3: 埋め込み再計算 | bge-m3で**Stage 0確定テキスト**（アップデート除外・NFKC済み）を再エンコード、L2正規化 | **旧キャッシュはアップデート込みテキスト由来**のため再利用すると入力が表現間で食い違う。全表現の入力を揃えるのが本計画の前提 |
| 1-5 | 品質確認の出力 | ①語彙サイズ・行列形状 ②ケース数件のTF-IDF上位語 ③全表現の行順一致チェック | チェックポイント材料。法律名・制度名が上位に来ていれば語彙系の質は担保 |

### 出力

- `plan02/features/tags.npz` / `tfidf.npz` / `count.npz` / `emb.npz` — 各表現行列（行順共通）
- `plan02/features/vocab.json` — A2の語彙リスト（インデックス→語）
- `plan02/features/case_index.json` — 行順の正（ケースID列）。以後の全ステージがこれを参照

### チェックポイント（依頼者に確認すること）

1. **TF-IDF上位語の品質**: 数ケースについて上位語を提示。「国民審査」「同性婚」「入管」など
   ケース固有の争点語が上位に来ているか（来ていなければ min_df/max_df・ストップワードを再調整）
2. 語彙サイズの妥当性（数千語程度を想定。数万なら2-gramの暴発、数百なら削りすぎのシグナル）

### 補足（設計判断の理由）

- **TF-IDFとCountを同一語彙で2本持つ**のは、後段でNMF（TF-IDF入力）とLDA（Count入力）の
  トピックを見比べるとき「語彙の違い」を交絡させないため。
- **埋め込みの再計算**はコストが小さい（95件・ローカルで数分）ため、入力統一の利益が上回る。
  新キャッシュは `plan02/features/` 配下に置き、plan01のものとは分離する。
- bag-of-tagsは「アーカイブ」等のシステムタグを除いた subject_tags のみ使用（Stage 0の凍結方針と同じ）。
