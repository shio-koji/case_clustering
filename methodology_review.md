# CALL4 訴訟ケース分類 — 方法論レビュー（未確定・網羅版）

> 本ドキュメントは「CALL4の公共訴訟を分類し、分かりやすく可視化する」という目的に対する
> **方法論の棚卸し**です。手法はまだ未確定。選択のための材料として、選択肢・長所短所・
> 用語解説・類題を網羅的に整理します。
> 作成: 2026-07-20

---

## 0. この検討の「芯」（依頼者確認済みの前提）

- **分類の形**: ハード排他クラスタより **混合メンバーシップ（soft membership / 基底ベクトル）** を志向。
  多ラベルOKだが「視覚的に表現できる」ことが条件。無理なら単一の基底ベクトル表現で満足。
  → 実質 **NMF / トピックのローディング** が最有力の落としどころ。
- **LLM活用**: LLMを使う版と使わない版の両方を試す。**力点はLLM不使用のデータ駆動**、必要なら補助。
- **既存タグ**: 「使う版（参照・答え合わせ）」と「全く使わない版（de novo）」の両方を試す。
- **主目的**: ①既存タグ体系の見直し提案 ②探索的理解・仮説生成 ③対外的な説明・可視化
  （※類似検索は主目的ではない）。

---

## 1. 論点1：高次元のままクラスタリングするのは妥当か

**結論：次元圧縮してからのクラスタリングの方が、この設定では明確に妥当。** 現状は「間違い」では
ないが最適でなく、しかもパイプライン内部に矛盾がある。

### 現状の実態
- k-means・Ward法は **1024次元の bge-m3 埋め込みのまま** 実行。
- HDBSCAN は **PCA-50次元**、TF-IDF系は **SVD-50次元**。
- → **手法ごとに入力空間の次元がバラバラ**。手法間比較（ARI/NMI）に「次元の差」が混入する。

### 圧縮すべき根拠
1. **N ≪ D**: 95件に対し1024次元。多くの方向はノイズの自由度。PCAはこれを捨てる**デノイズ**。
2. **距離集中（次元の呪い）**: 高次元では点間ユークリッド距離が均質化し弁別力が落ちる。
   k-meansはユークリッド前提なので直撃。PCA-30〜50で緩和。
3. **標準的な前処理との整合**: 「縮約→近傍/クラスタ」は一般的な前処理パイプラインと一致。
4. **公平な手法間比較**: 全手法を同じ縮約空間に揃えれば、ARI/NMIが「アルゴリズムの差」を測れる。

### 現状が「完全に無効」ではない理由
- 埋め込みはL2正規化済みで、k-meansは実質**球面k-means（コサイン）**に近くスケール問題は回避。
- Ward法は**コサイン距離**で回しており距離集中の影響は生ユークリッドよりマシ。
- 稀に識別に効く分散が低分散方向に潜む場合はPCAで落ちうる（本件で優先する理由は薄い）。

### 推奨
全クラスタリングを**共通の縮約空間**（例: 累積寄与率80–90%、目安30–50次元、その後再L2正規化）に
統一し、保持寄与率を明記。「1024次元 vs PCA-50」の実測比較を出すのが最も説得的。

---

## 2. 論点2：クラスタ名（例「C0: 外国にルーツを持つ人々 / ジェンダー…」）の作り方

**手順は単純で、テキストも特徴語も使っていない。** `03_analyze.py` の `name_cluster()`:

1. クラスタ内のケース群について**既存の人手タグ（subject_tags）の出現回数**を数える。
2. **出現数の多い上位2タグ**を `" / "` で連結する（＝多数決ラベル）。

### 方法論的な弱点（正直な評価）
- **循環的**: 「既存タグを問い直す」ためのデータ駆動クラスタを、まさにその既存タグで命名している。
- 特徴語（c-TF-IDF）は計算・表示しているが**命名には未使用**。しかも文字n-gramで `//` 等のノイズ語混入。
- plan01.md が求めた「特徴語・代表ケースを根拠にLLM命名（根拠併記）」は**未実装**。

### 改善方向
命名を「①クラスタ内外で差が大きい**語**（形態素ベースc-TF-IDF）＋②重心近傍の**代表ケース**」を
根拠に付ける。既存タグは「命名」ではなく「答え合わせ（クロス集計）」専用に回す。

---

## 3. 論点3：方法論の全体像（3つの設計軸）

目的（分類＋可視化）は、独立した3つの設計軸に分解できる。組み合わせが方法論になる。

### 軸A：特徴表現（何でケースを表すか）
| 表現 | メリット | デメリット |
|---|---|---|
| 既存タグのみ (bag-of-tags) | 解釈明快・null model | 人手バイアスを固定 |
| TF-IDF / BM25（語彙） | 透明・語が見える | 同義・言い換えに弱い |
| 文埋め込み（現行 bge-m3） | 意味的類似を捉える | ブラックボックス・命名困難 |
| **LLM構造抽出**（法分野/原告類型/被告=行政か/争点/救済/審級 …） | 解釈可能な多次元・多ラベル自然 | 抽出バイアス・要検証 |

### 軸B：構造の発見法
| 方法 | 特徴 | 向き/不向き |
|---|---|---|
| k-means / GMM | 高速・球状前提 | 排他的・k決め打ち |
| 階層 (Ward)＋デンドログラム | 入れ子＝タクソノミーが見える | N小でも可、後からk選択 |
| KNNグラフ＋Leiden/Louvain | グラフベース・解像度で粒度調整 | 橋渡し構造に強い |
| **トピックモデル / BERTopic / NMF / LDA** | **多ラベル自然・トピック語が自動** | 本件の落としどころ候補 |


### 軸C：可視化
- 2D投影: UMAP / t-SNE（現行）、PaCMAP / TriMap（大域構造を保つ）、PCA（線形・素直）
- デンドログラム（階層＝分類体系提案に最適）
- クラスタ×既存タグの Sankey / ヒートマップ（新旧対応、タグ見直しに直結）
- ネットワーク図（ケース=ノード、KNN=エッジ、クラスタ着色）
- ラベル付きトピック地図、ファセット検索UI

### フレーミング（軸の組み合わせ方＝本質）
1. **ボトムアップ教師なし（現行）**: 埋め込み→クラスタ→解釈。長所=de novo発見／短所=N小で不安定・命名が循環。
2. **トップダウンLLMタクソノミー**: LLMが体系提案→分類。長所=解釈・粒度制御・多ラベル／短所=客観性。
3. **参照ベース（タグ批評）**: 既存タグ主役、埋め込みで曖昧ケース・タグ重複・外れ値を発見。長所=タグ見直しに即効。
4. **多facet分類**: 単一分割を諦め、直交する複数軸（法分野/原告/被告/救済/審級）を別々に付与。長所=多面性に忠実。

---

## 4. 芯に沿った具体化：混合メンバーシップ（基底ベクトル）

「1ケース=1クラスタ」の排他分割を、

> **各ケース = K本の潜在軸（基底）への重みベクトル**（合計1に正規化）

に変えると、「多ラベル」と「単一の基底ベクトルで表現」が同時に成立する。

> 例：「この訴訟は 情報公開0.5 / 刑事司法0.3 / 環境0.2」のように混合比で表せる。

### 手法トラック（2×2：LLM有無 × タグ利用有無）

**Track A — LLM不使用・データ駆動【主軸】**
| 段階 | 手法 |
|---|---|
| 表現 | bge-m3埋め込み ＋ TF-IDF（※日本語は**形態素トークナイズ** MeCab/Sudachi に変更、文字n-gram脱却） |
| 次元圧縮 | PCA/SVDで縮約してから（論点1の是正） |
| 構造（排他・比較用） | Ward＋デンドログラム、Leiden（KNNグラフ）— 縮約空間で |
| 構造（混合・主役） | **NMF**（K本の非負トピック）→ 各ケース=トピック比率ベクトル。高荷重語で自己解釈可能 |
| 可視化 | 混合比スタックバー（積み上げ棒）／トピック×ケース ヒートマップ／UMAP（色=優勢トピック）／タグSankeyとARI/NMI |

長所: 客観的・再現可能・多ラベル自然・語で解釈可能。
短所: 日本語トピック語が粗くなりがち（形態素化で緩和）、Kの選択、命名は人手/LLM補助が要る。

**Track B — LLM補助【フォールバック／比較】**
- B1. LLM構造抽出 → 多facet特徴（＝解釈可能な多ラベル基底）
- B2. LLMタクソノミー誘導（全件要約→体系提案→再分類）
- 長所: 解釈性・多面性最大、命名容易／短所: 抽出バイアス、要検証、客観性は弱まる

**横断：既存タグの2版**
- 使う版: 参照ラベルとして混同分析・ARI/NMI・Sankey・「タグをまたぐ統合／1タグ内分裂」を定量指摘。
- 使わない版: de novoに基底を作り、最後にだけタグと対応づけて答え合わせ。

### 多ラベルを可視化する具体手法
| 可視化 | 見えるもの | 向き/限界 |
|---|---|---|
| **混合比スタックバー（積み上げ棒）** | 各ケースのK色積み上げ＝混合比が個票で一目 | 多ラベルの決定版。N=95なら全件並べ可 |
| トピック×ケース ヒートマップ | どのケースがどの軸に強いか | 全体パターン把握に最適 |
| UMAP + パイ/ドーナツ マーカー | 各点を小円グラフで混合比表示 | 地図＋多ラベル両立、密集に弱い |
| 三角図（simplex） | 3トピックの混合を三角座標で | K=3限定 |
| ネットワーク図（KNN、混合比で色ブレンド） | 近縁関係と橋渡しケース | 対外説明で直感的 |

> **「単一の基底ベクトル」への直球回答**: それは NMF/トピックのローディングそのもの。
> K≈6〜10の非負基底を作れば、多ラベル＝スタックプロットで可視化、が一括で成立する。

---

## 5. 推奨ロードマップ（この順で実装可能）

1. **論点1の是正＋実証**: 全クラスタリングを共通PCA/SVD空間に統一し、「1024次元 vs PCA-50」の
   シルエット・ARI差分を実測提示。
2. **Track A 主軸**: 形態素TF-IDF＋**NMF（Kスイープ 6〜10）** → スタックプロット＋トピック語＋
   （タグ使う/使わない両版の）対応。
3. **排他手法の並置**: Ward/Leidenを同一空間で回し、混合表現と比較（ARI/NMI・ブートストラップ安定性）。
4. **命名の是正（論点2）**: c-TF-IDF語＋代表ケース根拠で命名（LLMは任意）。
5. **Track B**: Aで解釈しづらい軸だけLLM facet抽出で補い、対応を検証。

---

## 付録A：用語解説（Glossary）

### 表現・前処理
- **埋め込み (embedding)**: テキストを意味を保った固定長の数値ベクトルに変換したもの。近い意味の文が
  近いベクトルになる（ケース × 次元 の行列）。
- **bge-m3**: 多言語・長文対応（最大8192トークン）の文埋め込みモデル。日本語に強く、ローカルで無料実行。
- **TF-IDF**: 「その文書で頻出（TF）かつ全体では珍しい（IDF）語」を重み付けする古典的な語彙特徴。透明だが
  同義語・言い換えに弱い。
- **BM25**: TF-IDFを改良した検索向けスコアリング。文書長を正規化。
- **c-TF-IDF (class-based TF-IDF)**: クラスタを1つの大きな文書とみなし、クラスタ内で特徴的な語を抽出する
  手法。クラスタを特徴づける語（説明語）を得るのに使う。
- **形態素解析 (morphological analysis)**: 日本語文を単語に分割する処理（MeCab, Sudachi等）。分かち書きの
  ない日本語で「語」を扱うのに必要。文字n-gram（連続する2〜4文字）は分割不要だがノイズ語が混じる。
- **L2正規化**: ベクトルの長さを1に揃える処理。ユークリッド距離をコサイン類似度と整合させる。
- **コサイン類似度**: 2ベクトルのなす角の余弦。長さでなく「向き」の近さを測る。テキスト類似の定番。

### 次元削減
- **PCA (主成分分析)**: 分散が最大の直交方向を順に取り、少数次元に線形圧縮する。デノイズ・高速化に使う。
- **SVD (特異値分解)**: 行列分解の一般手法。疎なTF-IDF行列にはTruncatedSVD（=LSA/潜在意味解析）を使う。
- **次元の呪い / 距離集中**: 高次元ほど点間距離が均質化し「近い/遠い」の差が消える現象。ユークリッド距離
  ベースの手法（k-means等）の弁別力を下げる。
- **UMAP / t-SNE**: 高次元データを2Dに落として可視化する非線形手法。**可視化専用**で、座標に過度な意味を
  持たせない。N小のときは近傍数(perplexity/n_neighbors)を小さく。
- **PaCMAP / TriMap**: UMAP/t-SNEの後継系。大域的な構造（クラスタ間の距離関係）をより保ちやすい。

### クラスタリング（排他的）
- **k-means**: k個の重心にケースを割り当て、重心を更新して収束させる。高速だが球状クラスタを仮定し、
  kを事前指定する必要がある。
- **GMM (混合ガウス)**: 各クラスタを正規分布とみなす。各点の所属を確率（soft）で出せる。
- **階層クラスタリング / Ward法**: ケースを順に併合して木構造（デンドログラム）を作る。Ward法は併合時の
  分散増加を最小化。後からどの高さで切るか（k）を選べ、入れ子構造が見える。
- **デンドログラム**: 階層クラスタリングの併合過程を示す樹形図。分類体系（タクソノミー）の提案に好適。
- **HDBSCAN**: 密度ベース。クラスタ数不要、まばらな点を「ノイズ(-1)」として除外できる。密度が薄いデータ
  では全ノイズ化することがある（→PCA縮約空間で回すと安定しやすい）。
- **Leiden / Louvain**: KNNグラフ上の**コミュニティ検出**。解像度パラメータで粒度を調整。
- **KNNグラフ (k近傍グラフ)**: 各ケースを最も近いk個と辺で結んだネットワーク。グラフ系クラスタリングの土台。

### 混合メンバーシップ（多ラベル）
- **混合メンバーシップ / soft membership**: 1ケースが複数のクラスタ/トピックに「比率」で属する表現。
  排他割り当て（hard）の対義。
- **NMF (非負値行列分解)**: 行列を「非負の基底 × 非負の重み」に分解する。各ケース＝トピックの**加法的な
  混合**として解釈でき、負が出ないため「部分の足し合わせ」として直感的。トピックごとの高荷重語が出る。
- **LDA (潜在ディリクレ配分)**: 確率的トピックモデル。各文書＝トピック分布、各トピック＝語分布。
- **BERTopic**: 「埋め込み→次元削減→クラスタリング→c-TF-IDF」をパッケージ化したトピックモデリング手法。
  本件で手作りしているパイプラインの製品版に近い。
- **archetypal analysis (原型分析)**: データの「極端な原型」の凸結合で各点を表す。混合表現の一種。
- **基底ベクトル**: ここでは「K本の潜在軸」を指す。各ケースをそのK軸への重みで表す＝混合メンバーシップ。

### 検証・評価
- **ARI (調整ランド指数)**: 2つの分割（例: データ駆動クラスタ vs 既存タグ）の一致度。偶然一致を補正。
  1=完全一致、0≈偶然、負=偶然以下。
- **NMI (正規化相互情報量)**: 2分割の共有情報量を0〜1に正規化。ラベル数が違っても比較しやすい。
- **シルエット係数**: 各点が「自クラスタにどれだけ密で、他クラスタからどれだけ離れているか」。-1〜1、
  高いほど良い。テキストの小Nでは低く出がち。
- **Davies-Bouldin指数**: クラスタ内分散とクラスタ間距離の比。低いほど良い。
- **gap統計量**: クラスタリングのまとまりを乱数基準と比較してkを選ぶ指標。
- **ブートストラップ安定性**: サブサンプリングを繰り返し、クラスタがどれだけ再現するか（ARI分布）で頑健性を評価。

### 可視化（多ラベル向け）
- **混合比スタックプロット（積み上げ棒）**: 各対象の成分（トピック）比率を積み上げ棒で示す図。
  各ケースのトピック混合比の可視化に使う。
- **Sankey図**: 左右のカテゴリ間（例: 既存タグ→データ駆動クラスタ）の流量を帯で示す。対応関係の可視化に。
- **simplex（三角図）**: 3成分の混合比を三角座標に配置。3トピック限定だが直感的。

### その他
- **多ラベル (multi-label)**: 1事例が複数カテゴリに同時に属しうる設定。訴訟は本質的に多ラベル。
- **教師なし / 教師あり**: ラベルを使わず構造を見つける（unsupervised）／既知ラベルで学習する（supervised）。
- **de novo**: 既知情報（既存タグ）に頼らずゼロから構造を立ち上げること。

---

## 付録B：一般的な類題（俯瞰）

今回の課題は「**小規模・多ラベル・既存の不完全な分類体系を持つ文書集合を、教師なし/半教師ありで再構造化し
可視化する**」という問題クラスに属する。同じ骨格を持つ代表的な類題:

### 直系（同じ骨格）
- **文書クラスタリング / トピックモデリング**: ニュース記事・論文・レビューを内容で束ねる。本件の一般形。
- **タクソノミー誘導 / オントロジー精緻化**: 既存の分類体系（タグ・カテゴリ）をデータから見直す。本件の主目的。
- **特許分類**: 特許を技術分野に分類（IPCコード）。既存コード体系の見直しは本件と同型。
- **判例検索・リーガルインフォマティクス**: 判例を争点・法分野で束ね、類似判例を検索。ドメインまで一致。

### 産業応用の類題
- **顧客セグメンテーション**: 購買・行動データで顧客を束ねる。多ラベル/soft割り当てが自然な点も共通。
- **EC商品カテゴリ整理**: 商品を自動分類し、既存カテゴリ体系の重複・欠落を発見。タグ見直しと同型。
- **推薦システム（類似アイテム）**: 埋め込み近傍で「似たもの」を返す。本件の副次タスク（類似ケース検索）。
- **ネットワークのコミュニティ検出**: SNS・引用網などをクラスタ化。Leiden/Louvainの本来の応用先。
- **画像クラスタリング**: 画像埋め込みを教師なしで束ねる。表現→縮約→クラスタの骨格が同じ。

### 統計・心理計測の類題
- **因子分析 / 主成分分析による潜在構造抽出**: アンケート項目の背後にある少数の潜在軸を見つける。→
  「基底ベクトル」発想の古典的原型。
- **多次元尺度構成法 (MDS)**: 対象間の距離関係を低次元に配置。UMAP/t-SNEの線形/古典版。

### 俯瞰まとめ
本件の位置づけを一言でいえば「**既存の不完全な分類体系を持つ文書集合を、教師なしで再構造化し、
多ラベル（混合メンバーシップ）として可視化する**」問題。既存タグは正解ではなく参照として扱い、
絶対視せず答え合わせに使う——という構図が、これら全ての類題に共通する背骨である。
