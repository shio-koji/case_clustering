# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# 仮想環境
.venv/
venv/
env/
ENV/

# キャッシュ・中間生成物
# 生データJSON（cases_list / case_* / updates_* / corpus*）はGitHubで閲覧できるよう追跡する
# 派生バイナリ（埋め込み・TF-IDF等のpickle）は大きく再生成可能なので除外
cache/*.pkl
*.cache

# 結果ファイル（HTMLレポート・解析結果は追跡する）
# Artifact公開用の派生HTML（本体レポートの重複なので除外）
plan01/results/report_artifact.html

# plan02: 中間成果物の派生バイナリは除外（JSON結果・レポートは追跡）
plan02/features/*.npz
plan02/features/*.pkl

# Jupyter
.ipynb_checkpoints/
*.ipynb

# 環境変数・シークレット
.env
.env.*
secrets.json

# MCP設定（APIキーなどが含まれる場合）
.mcp.json

# macOS
.DS_Store
.AppleDouble
.LSOverride

# エディタ
.vscode/
.idea/
*.swp
*.swo
*~

# ログ
*.log
logs/
