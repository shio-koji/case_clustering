# CLAUDE.md — CALL4 公共訴訟ケース クラスタリング解析プロジェクト
# CALL4 Public-Interest Litigation Case Clustering Project

> このファイルは Claude Code に本プロジェクトの目的・方針・制約を伝えるためのブリーフです。
> This file briefs Claude Code on the project's goals, approach, and constraints.
> 作業を始める前に全体を読み、不明点があれば着手前に質問してください。
> Read this fully before starting; ask before acting if anything is ambiguous.

---

## 0. TL;DR（最初に読む / read first）

- **やること / What**: CALL4（call4.jp）が扱う公共訴訟ケースのテキスト情報をMCP経由で取得し、**ベクトル埋め込み → クラスタリング**で分類を試みる、探索的データ解析プロジェクト。
- **成果物 / Deliverable**: 解析結果と**手法の丁寧な解説**を併記した**単一HTMLレポート**（日英併記）。
- **読者 / Audience**: 依頼者（オーナー）は**バイオインフォマティクスの基礎知識を持つ**。scRNA-seq のクラスタリングや次元削減に馴染みがある前提で、その語彙とのアナロジーを積極的に使ってよい。ただし手法の前提・限界は毎回明示すること。
- **スタンス / Stance**: 目的は「新しい分類軸の発見」「類似ケースの推薦・検索」を軸にした**探索的解析**。唯一の正解を出す作業ではなく、**複数手法を比較し、その差異自体を知見として提示する**。

---

## 1. 背景：CALL4とは / Context

CALL4 は日本初の「社会課題の解決を目指す訴訟（公共訴訟 / public-interest litigation, strategic litigation）」に特化した支援プラットフォーム（2019年〜、非営利運営）。掲載ケースには主題タグ（例：公正な手続、環境・災害、外国にルーツを持つ人々、ジェンダー・セクシュアリティ、刑事司法、情報公開 など）とステータス（active / archived / closed / no_donation）が付与されている。

**このプロジェクトの狙い**は、既存の人手タグ体系を絶対視せず、**ケースのテキスト（概要・主張・判決等）から立ち上がる潜在的な構造**をデータ駆動で可視化し、タグ体系の再考や類似ケース検索の基盤にすること。

> バイオインフォ的に言えば：既知のマーカー遺伝子ベースのアノテーション（＝人手タグ）に対して、**教師なしクラスタリングで de novo に細胞集団（＝ケース群）を見つけ直す**作業に近い。既知アノテーションは「答え合わせ」ではなく「参照」として使う。

---

## 2. データ / Data

### 2.1 取得元 / Source
- **CALL4 の MCP サーバは接続済みの前提**。まず MCP のツール一覧を確認し、取得できるフィールドを棚卸しすること。着手時に「どのツールで何が取れるか」を README なり作業ログに記録する。
- 想定される取得フィールド（実際に取れるものはMCPで確認）：ケースID、ケース名、概要文、原告の主張・訴訟の争点、判決・進捗、タグ、ステータス、提訴日・更新日、寄付額・支援者数 など。

### 2.2 解析の中心 / Primary signal
- **テキスト（概要・主張・判決など）を主たる特徴量とする。** 構造データ（タグ・審級・日付・寄付額）は**補助・検証・メタデータ**として使う（後述のバリデーションに有用）。

### 2.3 規模と含意 / Scale & implications
- ケース数は **おおよそ 100 件規模（小さい）**。これは手法選択に直結する重要な制約：
  - t-SNE / UMAP の `perplexity` / `n_neighbors` は小さめ（例：perplexity 5–15）にする必要がある。デフォルト（30）は N=100 では大きすぎる。
  - クラスタ数 k を大きく取ると各クラスタが数件になり解釈不能。**k は 4–8 程度から**始める。
  - ブートストラップやサブサンプリングでの安定性評価は有効だが、Nが小さいぶん分散が大きい点に留意。
  - 「統計的に有意なクラスタ」を主張しすぎない。**探索・仮説生成のためのツール**として扱う。

### 2.4 データ品質 / Quality notes
- 概要文の長さはケースによりばらつく（数十字〜数千字）。長文バイアスに注意（TF-IDF なら正規化、埋め込みなら基本問題ないが、極端な短文は情報量が低い）。
- 日本語テキスト。トークナイズ・正規化（全角半角、記号）に配慮。
- 欠損（概要なし・判決なし等）はドロップせず、**どのフィールドが欠けているかを特徴に含める / 記録する**。

---

## 3. 手法方針 / Methodology

### 3.1 埋め込み / Embeddings
- **計算方法は Claude Code の裁量で選んでよい**（依頼者の指定）。ただし以下を守ること：
  - **日本語テキストで意味的な質が出る手法を選ぶ。** 候補：多言語対応の文埋め込み（例：`intfloat/multilingual-e5-*`、`cl-nagoya/sup-simcse-ja`、OpenAI/Voyage 等のAPI、あるいは Claude/その他LLMの埋め込み）。ローカルかAPIかは、再現性・コスト・機密性のトレードオフで判断し、**選んだ理由をレポートに明記**。
  - 比較のため、**単純なベースライン（TF-IDF + SVD / bag-of-tags）も1つ計算**しておくと、埋め込みの付加価値が示せる。
  - 埋め込みは**キャッシュ**する（再計算コスト・API課金を避ける）。`.env` にAPIキー、成果物にはキーを絶対に含めない。

> バイオインフォ・アナロジー：埋め込み＝発現行列の取得。TF-IDFベースライン＝生カウント、文埋め込み＝バッチ補正・正規化済みの潜在表現、くらいの位置づけで語ると通じる。

### 3.2 前処理・次元削減 / Preprocessing & DR
- 埋め込みベクトルは L2 正規化を検討（コサイン類似度と整合）。
- **可視化用の次元削減（UMAP / t-SNE）と、クラスタリング用の空間を分けて考える。** クラスタリングは高次元の埋め込み上で行い、UMAP/t-SNE は**あくまで可視化**（座標に意味を持たせすぎない）とするのが安全。UMAP埋め込み上で直接クラスタリングする場合はその旨と危険性を明記。
- PCA/SVD での事前次元削減（例：50次元）→ 近傍計算、という scRNA-seq 的パイプラインは有効。

### 3.3 クラスタリング / Clustering
- **複数手法を試して比較する**こと（探索が目的）。最低限：
  - **k-means**（ベースライン、要 k 指定、球状クラスタ仮定）
  - **階層的クラスタリング（Ward等）+ デンドログラム**（k を後から選べる／入れ子構造が見える。タグ体系との対応を議論しやすい）
  - **HDBSCAN or Leiden/Louvain**（密度ベース／グラフベース。ノイズ点を許容、k 不要）。※ Leiden は scRNA-seq で標準なので依頼者に刺さる。KNNグラフ→コミュニティ検出の流れを採用してよい。
- **k の選択**は単一指標に頼らない：シルエット係数、Davies-Bouldin、gap statistic、デンドログラムの見た目、そして**解釈可能性**を総合。「指標が最良でも解釈不能なら不採用」という判断を明示的に書く。

### 3.4 クラスタの解釈 / Cluster interpretation
- 各クラスタについて：
  - 代表ケース（重心/メドイドに近いもの）を数件挙げる。
  - **特徴語**（クラスタを特徴づける単語）を抽出（例：クラスタ内 vs 外の TF-IDF 差、c-TF-IDF 等）。
  - **既存タグとの対応表**（クラスタ × タグのクロス集計 / ヒートマップ）。「クラスタ3は既存タグX・Yにまたがる」等を定量化。
  - LLM（Claude Code 自身）に各クラスタの命名・要約をさせてよいが、**根拠（どのケース・特徴語からその名にしたか）を必ず併記**。ハルシネーション防止。

> バイオインフォ・アナロジー：特徴語抽出＝差次的発現遺伝子（DEG）解析、クラスタ命名＝マーカーに基づく細胞型アノテーション。既存タグとのクロス集計＝リファレンスラベルとのconfusion的な対応づけ。

### 3.5 バリデーション / Validation
- **安定性**：異なる乱数シード・サブサンプル・手法間でクラスタがどれだけ一致するか（ARI / NMI で手法間・シード間比較）。
- **既存タグとの一致度**：ARI / NMI で「データ駆動クラスタ vs 人手タグ」を測る。**一致しすぎ＝タグの焼き直し、乖離しすぎ＝ノイズor新発見**、どちらかを解釈する。
- **推薦タスクの妥当性**：類似ケース検索（後述）を数例やって、人間の直感と合うか定性チェック。

---

## 4. 応用タスク / Downstream tasks

主目的は「新しい分類軸の発見」「類似ケースの推薦・検索」。探索の中で以下を作る：

1. **類似ケース検索**：任意のケースに対しコサイン類似で近傍 top-k を返す。レポートに数例を掲載（「このケースに似た訴訟」）。
2. **新分類軸の提案**：クラスタリング結果と既存タグの対応から、「既存タグでは分かれていないが実は別クラスタ」「複数タグをまたぐ統合クラスタ」を具体的に指摘。
3. （余力があれば）**2次元マップ**上に全ケースを配置し、クラスタ・既存タグ・ステータスを色分けして俯瞰。

---

## 5. 成果物 / Deliverable

### 5.1 形式
- **単一の自己完結HTMLレポート**（外部依存は最小限。図は inline SVG かデータ埋め込みで、ローカルで開けること）。メンバー間で共有するため、1ファイルで完結するのが望ましい。
- 図はインタラクティブでも静的でもよいが、**静的でも意味が通る**こと（印刷・スクショ共有を想定）。

### 5.2 言語 / Language
- **日英併記**。見出し・図キャプション・主要な解説は日英両方。細かい注記は日本語主・英語従でも可。コード内コメントは英語で可。

### 5.3 レポートに必ず含める要素 / Must-haves
1. **データ概要**：件数、使ったフィールド、欠損状況、前処理。
2. **手法の解説**：埋め込み・次元削減・各クラスタリング手法について、**なぜそれを使うのか／前提／限界**を、バイオインフォ既知の読者向けに丁寧に。数式は最小限、直感を優先しつつ正確に。
3. **結果**：各手法のクラスタ、可視化、特徴語、命名。
4. **手法間・タグとの比較**：ARI/NMI表、クロス集計ヒートマップ。
5. **解釈と示唆**：新分類軸の候補、類似検索の例、タグ体系への含意。
6. **限界と次の一手**：N の小ささ、埋め込みの選択依存性、再現性の注意。**過剰主張を避ける明示的な但し書き**。
7. **再現情報**：使ったモデル名・バージョン・パラメータ・乱数シード。

### 5.4 トーン / Tone
- 依頼者はデータ解析の作法を知っている。**手取り足取りにしすぎず、しかし前提と限界は毎回明示**。「なぜこの手法か」「何を仮定しているか」「何を主張できて何を主張できないか」を各所で言語化する。誠実な不確実性の表明を歓迎する。

---

## 6. 技術・運用ルール / Engineering & ops

- **言語/環境**：Python 想定（依頼者のバイオインフォ経験に整合）。`scikit-learn`, `umap-learn`, `hdbscan`, `scipy`, `sentence-transformers` 等。仮想環境を切ること。
- **秘密情報**：APIキー・トークンは `.env` に置き、リポジトリ／成果物に含めない。取得したケーステキストの再配布可否は不明なので、**生データを成果物HTMLに大量転載しない**（要約・特徴語・数件の抜粋に留める）。
- **キャッシュ**：MCP取得結果と埋め込みはローカルにキャッシュし、再取得・再課金を避ける。取得日時を記録。
- **再現性**：乱数シード固定。パラメータは設定ファイル or レポート冒頭にまとめる。
- **段階的に進める**：①MCPでデータ取得・棚卸し → ②前処理・埋め込み（+キャッシュ）→ ③可視化 → ④複数手法クラスタリング → ⑤解釈・比較 → ⑥HTML化。各段階で中間確認できるようにする。
- **著作権・倫理**：ケースには実在の当事者・係争が含まれる。当事者のセンシティブな記述の扱いは慎重に。分類は解析目的であり、当事者を類型化して評価する意図はない旨をレポートに明記。

---

## 7. 着手前チェックリスト / Before you start

1. MCP サーバに接続できるか、どのツール・フィールドが取れるかを確認したか？
2. 取得できるテキストフィールドの実際の中身（長さ・欠損）を数件目視したか？
3. 埋め込み手法の選択肢とコスト・機密性を検討し、方針を決めたか？（決めたら理由を記録）
4. N が小さい前提でのパラメータ方針（perplexity, k, min_cluster_size）を立てたか？
5. 不明点・判断に迷う点をオーナーに確認したか？

> **迷ったら止まって聞く。** 特に「どのフィールドを主特徴量にするか」「埋め込み手法」「生テキストの成果物への掲載範囲」は、勝手に決めず確認するのが望ましい。

---

## 8. 用語対応（バイオインフォ ↔ 本プロジェクト）/ Glossary bridge

| バイオインフォ | 本プロジェクト |
|---|---|
| 発現行列 (cells × genes) | 埋め込み行列 (cases × dims) |
| 正規化・バッチ補正 | テキスト正規化・埋め込み選択 |
| HVG選択・PCA | 特徴選択・次元削減 (SVD/PCA) |
| KNNグラフ + Leiden | KNN + コミュニティ検出でケースをクラスタ化 |
| UMAP可視化 | UMAP/t-SNEでケースマップ |
| マーカー遺伝子 / DEG | クラスタ特徴語 (c-TF-IDF等) |
| 細胞型アノテーション | クラスタ命名（既存タグ参照＋LLM要約） |
| リファレンスマッピング | 既存タグとのARI/NMI比較 |

このアナロジーはレポート内でも読者の理解を助けるので、適宜活用してよい（ただし比喩に頼りすぎて厳密性を失わないこと）。